export interface Quote {
    character: string;
    id?: string;
    quote: string;
}
export interface QuoteLoadMatch {
    id: number;
}
export interface QuoteListMatch {
    character?: string;
    id?: string;
    quote?: string;
}
